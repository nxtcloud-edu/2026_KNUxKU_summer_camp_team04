fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
pydantic==2.10.4
pydantic-settings==2.7.0

# 인증
bcrypt==5.0.0
PyJWT==2.13.0
email-validator==2.3.0   # pydantic 의 EmailStr 이 요구한다

# dev/test
pytest==8.3.4
httpx==0.28.1

# NOTE: sqlalchemy는 직접 pin하지 않는다. sqlmodel이 호환 버전을 해결한다.
# NOTE: docker SDK는 여기 없다. app/judge/docker_judge.py가 함수 내부에서 지연 import하므로
#       JUDGE_BACKEND=docker를 켤 때만 `pip install "docker>=7.0"`을 추가하면 된다.
