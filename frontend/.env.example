# 백엔드 주소. 문제 조회 · 채점 · Coding Trace 수집을 전부 여기로 보낸다.
#
# 예전에는 judge 서버(judge/main.py)를 가리켰지만 이제 백엔드 하나로 통합됐다.
# 백엔드가 내부에서 Docker judge 를 호출하므로 채점 결과는 동일하다.
#
# 실행: cd backend && uvicorn app.main:app --port 8000 --workers 1
VITE_API_BASE_URL=http://localhost:8000
